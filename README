My vim environment & plugins. Plugins incorporated wih git submodule.
To use this:

mkdir ~/.vim
cd ~/.vim
git clone git@github.com:shumphrey/Vim-Settings.git

You can also use this vimrc file
ln -s ~/.vim/vimrc ~/.vimrc
